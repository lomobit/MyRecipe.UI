{
  "name": "configurator",
  "version": "1.0.0",
  "description": "",
  "main": "dist/configurator.js",
  "scripts": {
    "start": "node dist/configurator.js",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "rebuild": "tsc --build --clean && tsc --build",
    "rebuild-start": "tsc --build --clean && tsc --build && node dist/configurator.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.15.5",
    "typescript": "^5.0.2"
  }
}
